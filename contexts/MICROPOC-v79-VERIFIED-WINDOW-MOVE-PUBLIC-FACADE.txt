RumiAI Computer Control micro-PoC v79
VERIFIED WINDOW MOVE PUBLIC FACADE

STATUS
VALIDATED

PURPOSE
Expose validated v78 move through public ComputerControl.moveWindow({app, window, position}).

CONTRACT
Preserve the complete descriptor and numeric position {x,y}; route only through desktop.moveWindow; success requires MOVED, moved=true and verified=true; propagate bounds, previousBounds, desiredBounds, position, verificationMethod=native-ax-window-position and handle diagnostics; fail closed without native fallback.

VALIDATION
Boundary plus physical public-facade test proving requested position, preserved size, independent observation, restoration and runtime shutdown.

NEXT
After PASS, v80 may implement only Desktop Plugin resizeWindow.

RESULTS
- Boundary PASS: `fb43e76`.
- Physical harness: `0ef4566`.
- Physical PASS: `1c09544`.

CONCLUSION
Public moveWindow is physically validated with complete descriptor routing, requested position, preserved size and independent verification.
