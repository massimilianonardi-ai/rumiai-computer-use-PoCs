RumiAI Computer Control micro-PoC v81
VERIFIED WINDOW RESIZE PUBLIC FACADE

STATUS
VALIDATED

PURPOSE
Expose validated v80 resize through public ComputerControl.resizeWindow({app, window, size}).

CONTRACT
Preserve the complete descriptor and numeric positive size {width,height}; route only through desktop.resizeWindow; success requires RESIZED, resized=true and verified=true; propagate bounds, previousBounds, desiredBounds, size, verificationMethod=native-ax-window-size and handle diagnostics; fail closed without native fallback.

VALIDATION
Boundary plus physical public-facade test proving requested size, preserved position, independent observation, restoration and runtime shutdown.

NEXT
After PASS, v82 must execute the complete physical public Window Control regression and close the phase.

RESULTS
- Boundary PASS: `774332e`.
- Physical harness: `d21e828`.
- Physical PASS: `4695dfc`.

CONCLUSION
Public resizeWindow is physically validated with requested size, preserved position, complete descriptor routing and independent AX verification.
