RumiAI Computer Control micro-PoC v61
WINDOW LIST FACADE BOUNDARY

STATUS
VALIDATED

PURPOSE
Expose the v60-validated Desktop Plugin window-list observation through the public Computer Control facade without leaking backend or OS-specific implementation details.

ARCHITECTURAL INVARIANT
ComputerControl.listWindows({app}) owns only the normalized public contract and application resolution.
The selected Desktop Plugin owns HOW windows are enumerated.
agent-ctrl remains private to the macOS plugin/backend boundary.

SCOPE
- Add public ComputerControl.listWindows({app}).
- Require app.
- Resolve the application Provider.
- Resolve the selected Desktop Plugin application identity.
- Route only through desktop.listWindows(application).
- Require an observed windows array.
- Return normalized OBSERVED state, windows, method and observation timing.
- Preserve the already validated v60 backend/plugin implementation unchanged.

NON-GOALS
- No new window enumeration backend behavior.
- No focusWindow implementation.
- No minimize/maximize/restore/move/resize implementation.
- No planner/executor changes.
- No Windows/Linux implementation claims.
- No semantic changes to getCurrentWindow or closeWindow.

VALIDATION RESULT
Static boundary initially failed because the test searched for `snapshotApplication(` across the entire Computer Control facade, where that backend reference legitimately pre-existed in readiness/synchronization code. The failure was committed before diagnosis. The boundary test was corrected to isolate the body of listWindows(); the implementation itself was unchanged.

Corrected static boundary: PASS.
Physical facade validation: PASS on macOS.

Physical evidence:
- runtime-ready=PASS
- two real TextEdit fixture windows opened
- application-ready=PASS
- facade-window-list=PASS
- facade-window-list-state=OBSERVED
- facade-window-count=2
- both fixture titles returned through ComputerControl.listWindows({app:"TextEdit"})
- distinct window ids returned
- normalized id/title/process/pid/focused/pinned fields verified
- pinned window verified
- physical-window-list-facade=PASS
- runtime-close=PASS
- physical_exit=0
- fixture-cleanup=WARN recorded as test-only cleanup warning; it did not affect the observation result or exit status

CONCLUSION
v61 VALIDATED.
Public ComputerControl.listWindows({app}) now preserves the Desktop Plugin boundary while exposing the physically validated v60 window enumeration behavior.
