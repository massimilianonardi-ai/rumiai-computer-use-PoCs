"use strict";
const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");const test=require("node:test");
const portableRoot=path.resolve(__dirname,"../../../../../..");const productRoot=process.env.RUMIAI_COMPUTER_CONTROL_ROOT||path.join(portableRoot,"lib","computer-control");
const fixture=path.resolve(__dirname,"../fixtures/macos-appkit-file-picker-discovery/main.swift");
const probe=path.resolve(__dirname,"../physical-tests/helpers/macos-file-picker-ax-topology.swift");
const physical=path.resolve(__dirname,"../physical-tests/macos-file-picker-topology-discovery.js");

test("Phase 9B3A discovery used a real NSOpenPanel and system-wide AX observation",()=>{const fixtureSource=fs.readFileSync(fixture,"utf8");const probeSource=fs.readFileSync(probe,"utf8");const physicalSource=fs.readFileSync(physical,"utf8");assert.match(fixtureSource,/NSOpenPanel\(\)/);assert.match(fixtureSource,/beginSheetModal/);assert.match(fixtureSource,/canChooseFiles\s*=\s*true/);assert.match(fixtureSource,/canChooseDirectories\s*=\s*true/);assert.match(probeSource,/AXUIElementCreateSystemWide/);assert.match(probeSource,/kAXFocusedApplicationAttribute/);assert.match(probeSource,/NSWorkspace\.shared\.runningApplications/);assert.match(physicalSource,/phase9b3a-topology-json/);});

test("Phase 9B3A discovery treats global focus as optional evidence, not picker identity",()=>{const physicalSource=fs.readFileSync(physical,"utf8");assert.match(physicalSource,/Array\.isArray\(value\?\.candidates\)&&value\.candidates\.length/);assert.match(physicalSource,/phase9b3a-focused-application=OBSERVED/);assert.doesNotMatch(physicalSource,/check\("phase9b3a-focused-application/);assert.match(physicalSource,/MARKERS\.every\(marker=>candidate\.markerMatches\.includes\(marker\)\)/);});

test("Phase 9B3A discovery evidence is preserved and informs the product contract",()=>{const evidence=fs.readFileSync(path.join(productRoot,"docs/evidence/phase9b3a-file-picker-discovery.md"),"utf8");assert.match(evidence,/326f3283da91ee4c32a7d67bd8bb6e55b414d9ce/);assert.match(evidence,/22 PASS \/ 0 FAIL \/ 0 BLOCKED/);assert.match(evidence,/accessible picker surface was visible under that same Provider process/);assert.match(evidence,/global.*FocusedApplication.*not a valid identity prerequisite/i);assert.match(evidence,/open-panel/);assert.match(evidence,/ListView/);assert.match(evidence,/where popup/);});
