"use strict";
const assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),test=require("node:test");
const portableRoot=path.resolve(__dirname,"../../../../../..");const productRoot=process.env.RUMIAI_COMPUTER_CONTROL_ROOT||path.join(portableRoot,"lib","computer-control");
test("Phase 9B3B disclosure discovery probes only native outline disclosure semantics",()=>{const helper=fs.readFileSync(path.join(__dirname,"../physical-tests/helpers/macos-file-picker-directory-disclosure.swift"),"utf8");const harness=fs.readFileSync(path.join(__dirname,"../physical-tests/macos-file-picker-directory-disclosure-discovery.js"),"utf8");assert.match(helper,/kAXDisclosureTriangleRole/);assert.match(helper,/kAXPressAction/);assert.match(helper,/kAXDisclosingAttribute/);assert.doesNotMatch(helper,/kAXConfirmAction|AXOpen|CGEvent|keyCode|NSEvent|mouse/);assert.match(harness,/observeFilePicker/);assert.match(harness,/Nested\.txt/);assert.match(harness,/independent-observation/);});
test("Phase 9B3B disclosure discovery remains non-promoting after adopting its model",()=>{const backend=fs.readFileSync(path.join(productRoot,"backends/macos/backend.js"),"utf8");assert.match(backend,/filePicker\.selectItem.*IMPLEMENTED/);assert.match(backend,/filePicker\.expandDirectory.*IMPLEMENTED/);assert.doesNotMatch(backend,/filePicker\.openDirectory/);assert.match(backend,/filePicker\.observe.*PHYSICALLY_VALIDATED/);});
