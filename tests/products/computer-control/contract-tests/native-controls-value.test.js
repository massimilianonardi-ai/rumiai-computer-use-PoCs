"use strict";
const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");const test=require("node:test");
const portableRoot=path.resolve(__dirname,"../../../../../..");const productRoot=process.env.RUMIAI_COMPUTER_CONTROL_ROOT||path.join(portableRoot,"lib","computer-control");const {createRouter}=require(path.join(productRoot,"runtime/src/router"));const {createMacOSBackend}=require(path.join(productRoot,"backends/macos/backend"));
function fixture(){return{info:async()=>({name:"fixture",version:"0",platform:"macos",capabilities:[]}),setValue:async({target,value})=>({state:"VALUE_SET",verified:true,target,requestedValue:value,observedValue:value}),increment:async({target})=>({state:"INCREMENTED",verified:true,target,previousValue:5,observedValue:6}),decrement:async({target})=>({state:"DECREMENTED",verified:true,target,previousValue:6,observedValue:5})};}
test("value mutations route canonical parameters",async()=>{const route=createRouter(fixture());assert.equal((await route("ui.setValue",{application:"NativeFixture",target:{ref:"@e1"},value:7})).state,"VALUE_SET");assert.equal((await route("ui.increment",{application:"NativeFixture",target:{ref:"@e1"}})).state,"INCREMENTED");assert.equal((await route("ui.decrement",{application:"NativeFixture",target:{ref:"@e1"}})).state,"DECREMENTED");await assert.rejects(route("ui.setValue",{application:"NativeFixture",target:{ref:"@e1"},value:null}),e=>e.code==="CONTROL_VALUE_REQUIRED");});
test("value capabilities await AppKit physical evidence",async()=>{const info=await createMacOSBackend({backendModule:{}}).info();for(const name of ["ui.setValue","ui.increment","ui.decrement"])assert.equal(info.capabilities.find(x=>x.name===name)?.validationState,"IMPLEMENTED");});
test("SDK and schemas preserve value surfaces",()=>{const setSchema=JSON.parse(fs.readFileSync(path.join(productRoot,"contract/schemas/set-value.params.schema.json"),"utf8"));const sdk=fs.readFileSync(path.join(productRoot,"sdk/typescript/src/index.js"),"utf8");const types=fs.readFileSync(path.join(productRoot,"sdk/typescript/src/index.d.ts"),"utf8");assert.deepEqual(setSchema.required,["application","target","value"]);assert.match(sdk,/setValue\(\{application, target, value, settle = true\}\)/);assert.match(sdk,/increment\(\{application, target, settle = true\}\)/);assert.match(sdk,/decrement\(\{application, target, settle = true\}\)/);assert.match(types,/interface ValueMutationResult/);});
