"use strict";
const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");const test=require("node:test");
const portableRoot=path.resolve(__dirname,"../../../../../..");const productRoot=process.env.RUMIAI_COMPUTER_CONTROL_ROOT||path.join(portableRoot,"lib","computer-control");
const {createRouter}=require(path.join(productRoot,"runtime/src/router"));
function backend(){return{listApplications:async p=>({method:"application.list",...p}),launchApplication:async p=>({method:"application.launch",...p}),activateApplication:async p=>({method:"application.activate",...p})};}

test("Phase 9A1 router validates application lifecycle inputs before dispatch",async()=>{const route=createRouter(backend());const listed=await route("application.list",{});assert.equal(listed.method,"application.list");assert.equal(listed.availableOnly,false);const filtered=await route("application.list",{availableOnly:true});assert.equal(filtered.availableOnly,true);await assert.rejects(route("application.list",{availableOnly:"yes"}),e=>e.code==="INVALID_AVAILABLE_ONLY");for(const method of["application.launch","application.activate"]){await assert.rejects(route(method,{}),e=>e.code==="APP_REQUIRED");await assert.rejects(route(method,{application:"  "}),e=>e.code==="APP_REQUIRED");await assert.rejects(route(method,{application:"Fixture",timeoutMs:1}),e=>e.code==="INVALID_TIMEOUT");const value=await route(method,{application:"Fixture",timeoutMs:5000});assert.equal(value.application,"Fixture");assert.equal(value.timeoutMs,5000);}});

test("Phase 9A1 public surfaces are provider-scoped and keep terminate deferred",()=>{const lifecycle=fs.readFileSync(path.join(productRoot,"backends/macos/application-lifecycle.js"),"utf8");const backendSource=fs.readFileSync(path.join(productRoot,"backends/macos/backend.js"),"utf8");const sdk=fs.readFileSync(path.join(productRoot,"sdk/typescript/src/index.js"),"utf8");const types=fs.readFileSync(path.join(productRoot,"sdk/typescript/src/index.d.ts"),"utf8");const adapter=fs.readFileSync(path.join(productRoot,"adapters/rumiai/compat.js"),"utf8");assert.match(lifecycle,/providerForApplication/);assert.match(lifecycle,/providerResolvedPath/);assert.match(lifecycle,/desktop\.launchApplication/);assert.match(lifecycle,/desktop\.activateApplication/);assert.match(lifecycle,/\/usr\/bin\/pgrep/);assert.doesNotMatch(lifecycle,/execSync|shell:\s*true|pkill|kill\s|terminateApplication/);assert.match(backendSource,/application\.list/);assert.match(backendSource,/application\.launch/);assert.match(backendSource,/application\.activate/);assert.doesNotMatch(backendSource,/application\.terminate/);assert.match(sdk,/listApplications\(/);assert.match(sdk,/launchApplication\(/);assert.match(sdk,/activateApplication\(/);assert.match(types,/interface ApplicationDescriptor/);assert.match(adapter,/listApplications/);});

test("Phase 8C is promoted while Phase 9A1 remains IMPLEMENTED",()=>{const backendSource=fs.readFileSync(path.join(productRoot,"backends/macos/backend.js"),"utf8");for(const name of["ui.replaceTextRange","ui.insertText","ui.appendText"])assert.match(backendSource,new RegExp(name.replace(".","\\.")));assert.match(backendSource,/PHASE8C/);for(const name of["application.list","application.launch","application.activate"])assert.match(backendSource,new RegExp(name.replace(".","\\.")));assert.match(backendSource,/validationState:"IMPLEMENTED"/);});

test("Phase 9A1 schemas and documentation define the provider-scoped safety boundary",()=>{for(const file of["application-list.params.schema.json","application-lifecycle.params.schema.json","application-list-result.schema.json","application-lifecycle-result.schema.json"]){JSON.parse(fs.readFileSync(path.join(productRoot,"contract/schemas",file),"utf8"));}const docs=fs.readFileSync(path.join(productRoot,"docs/api-application-lifecycle.md"),"utf8");assert.match(docs,/registered application Providers/);assert.match(docs,/does not launch implicitly/);assert.match(docs,/application\.terminate.*deferred/i);});
