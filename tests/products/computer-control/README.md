# Conformance

Every promoted capability requires:

1. schema/contract tests;
2. backend unit tests;
3. shared semantic conformance scenarios;
4. physical evidence on the target OS.

Results are recorded per backend, backend version, OS version, application, and
surface. `available` is never interpreted as global universal support.
